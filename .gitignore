# ============================================================
# ビルド成果物
# ============================================================
/target/
**/*.rs.bk
*.pdb

# ============================================================
# OS / エディタ / ローカルツール設定
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/
.claude/

# ============================================================
# シークレット (順序が重要: .env.* を落としてから example を戻す)
# ============================================================
.env
.env.*
!.env.example

# ============================================================
# 著作権物: ディスクイメージは絶対にコミットしない
# ------------------------------------------------------------
# ディレクトリ丸ごと deny + .gitkeep のみ allow。
# 拡張子の大文字小文字 (X68000 の実イメージは *.HDS / *.IMG が普通) と
# サブディレクトリ配置のどちらにも影響されない。
# 意図的に追加したい場合のみ `git add -f` を使うこと。
# ============================================================
/tests/data/*
!/tests/data/.gitkeep

# tests/data/ 以外に置かれた場合の保険 (再帰・大小文字の両方)
*.xdf
*.XDF
*.img
*.IMG
*.hds
*.HDS
*.hdf
*.HDF
*.dim
*.DIM
*.d88
*.D88
*.2hd
*.2HD
*.lzh
*.LZH

# ============================================================
# 索引・要約・抽出物
# ------------------------------------------------------------
# tantivy 索引は原本の逐語テキストを保持し、summaries/ は著作物の
# AI 要約を保持するため、いずれも公開不可。
# README がリポジトリ直下に ./archive ./index を作る手順を案内するので
# ここで明示的に落とす。
# ============================================================
/archive/
/index/
/downloads/
/out/
/_index_test/
/xdf-extracts/
**/summaries/

# ============================================================
# ユーザ生成の設定ファイル
# ============================================================
/xdf-grouping.toml
!/xdf-grouping.example.toml

# ============================================================
# テスト用 PNG ダンプ
# ============================================================
/_pic_*.png
/_*.png

# ============================================================
# AI 生成の計画ドキュメント (管理外)
# ============================================================
/docs/requirements.md
/docs/design.md
/docs/tasks.md
/docs/additional_spec/
/CLAUDE.md
